-- Selecciona todos los registros de la tabla 'users'
SELECT * FROM users;

-- Selecciona el campo 'name' de todos los registros de la tabla 'users'
SELECT name FROM users;

-- Selecciona los campos 'name' y 'email' de todos los registros de la tabla 'users'
SELECT name, email FROM users;