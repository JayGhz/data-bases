-- Seleccionar todos los usuarios ordenados por su edad
SELECT * FROM users ORDER BY age;

-- Seleccionar todos los usuarios ordenados por su edad de forma descendente
SELECT * FROM users ORDER BY age DESC;

-- Seleccionar todos los usuarios con nombre 'ArianaMs' ordenados por su edad
SELECT * FROM users WHERE name='ArianaMS' ORDER BY age DESC;